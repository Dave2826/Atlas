@model Atlas.Models.Entities.Size

@{
    ViewData["Title"] = "Desactivar Talla";
}

<h1>Desactivar Talla</h1>

<h3>¿Está seguro que desea desactivar esta talla?</h3>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Nombre</dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">Descripción</dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
        <dt class="col-sm-2">Orden</dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.DisplayOrder)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="SizeId" />
        <input type="submit" value="Desactivar" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
    </form>
</div>
